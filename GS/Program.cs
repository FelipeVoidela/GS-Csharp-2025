using System;

namespace GS
{
    // Ponto de entrada da aplicação
    class Program
    {
        static void Main(string[] args)
        {
            AppSistema app = new AppSistema();
            app.Iniciar();  // Inicia o sistema e mostra menu para o usuário
        }
    }
}
